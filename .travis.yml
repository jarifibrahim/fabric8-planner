sudo: required
language: node_js
addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable
node_js:
  - "8"
cache: npm
install:
 - npm install
 - npm run build
 - cd runtime
 - npm install
script:
 - HEADLESS_MODE=true npm run test:funcsmoke